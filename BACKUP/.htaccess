<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^(?:app|database|\.git|\.agents|\.codex)(?:/|$) - [F,L]

    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
