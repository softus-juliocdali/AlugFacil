# Split Asaas — legado histórico

Estrutura criada nos Prompts 07/07.0 e descontinuada pelo 07-R. Novas reservas
não leem nem escrevem em `asaas_splits` e não enviam `split` ou `splits`.

Migration, aplicador e verificador são preservados somente para documentar o
schema histórico. Não existe executor, rota, job ou flag ativa de split.
